/**  @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // toggle por clase funciona con JS
  content: ["./*.html", "./*.js"], // Tailwind escanea estos archivos para generar clases
  theme: {
    extend: {},
  },
  plugins: [],
}